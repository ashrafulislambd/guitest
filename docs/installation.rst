.. highlight:: shell

============
Installation
============


Stable release
--------------

To install guiprint, run this command in your terminal:

.. code-block:: console

    $ pip install guiprint

This is the preferred method to install guiprint, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for guiprint can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/ashrafulislambd/guiprint

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/ashrafulislambd/guiprint/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/ashrafulislambd/guiprint
.. _tarball: https://github.com/ashrafulislambd/guiprint/tarball/master
